{
  "schema_version": 1,
  "kind": "sqlite-authoritative-mutation-route-inventory",
  "implementation": "tools/sqlite_storage.py",
  "contract_sha256": "d8ca82e7cdc0cf102b888b12a23176fbf0fb3570515b2d972f201198ff006e65",
  "routes": [
    {
      "method": "mutate",
      "operations": ["task transition", "dependency rewrite", "event append"],
      "gate": "SQLiteBackend.transaction"
    },
    {
      "method": "update_observations",
      "operations": ["observation update", "reconciliation event append"],
      "gate": "SQLiteBackend.transaction"
    },
    {
      "method": "append_command_result",
      "operations": ["command result append"],
      "gate": "SQLiteBackend.transaction"
    },
    {
      "method": "retire",
      "operations": ["authority retirement", "selector switch callback"],
      "gate": "SQLiteBackend.transaction"
    }
  ],
  "nonclaims": [
    "Static route coverage does not prove runtime refinement or authorize upgrade mutation.",
    "The inventory does not cover read-only connections or future Git authority routes."
  ]
}
